# WARNING WARNING WARNING WARNING WARNING
# DO NOT MODIFY THIS FILE!! This file is generated!
# WARNING WARNING WARNING WARNING WARNING

import logging
import std.xml as xml
import stratoweave.ttt as ttt
import yang.adata as yadata
import yang.gdata as ygdata
import yang.xml as yxml

from sorespo.layers.y_0 import stratoweave_tmf__tmf_store__tmf640__service_entry
from sorespo.layers.y_0 import ietf_l3vpn_svc__l3vpn_svc__vpn_services__vpn_service_entry
from sorespo.layers.y_0 import ietf_l3vpn_svc__l3vpn_svc__sites__site_entry
from sorespo.layers.y_0 import netinfra__netinfra__router_entry
from sorespo.layers.y_0 import netinfra__netinfra__roadm_entry
from sorespo.layers.y_0 import netinfra__netinfra__backbone_link_entry
from sorespo.layers.y_0 import netinfra__netinfra__optical_link_entry
from sorespo.layers.y_0 import netinfra__netinfra__global_settings
from sorespo.layers.y_0_oper import ietf_l3vpn_svc__l3vpn_svc__sites__site_entry as ietf_l3vpn_svc__l3vpn_svc__sites__site_entry__oper
from sorespo.layers.y_0_oper import netinfra__netinfra__backbone_link_entry as netinfra__netinfra__backbone_link_entry__oper
from sorespo.layers.y_0 import SRC_DNODE
from sorespo.layers.y_1_loose import root as output_root

def o_root():
    return output_root()


class Tmf640Store(ttt.TransformFunction):
    transform: mut(stratoweave_tmf__tmf_store__tmf640__service_entry) -> yadata.MNode

    @staticmethod
    def input_type():
        """Return the modeled input type for this transform"""
        return stratoweave_tmf__tmf_store__tmf640__service_entry

    mut def transform_wrapper(self, i: ygdata.Node, linked: ygdata.Node, memory: ?ygdata.Node, dynstate: ?ygdata.Node) -> (ygdata.Node, ?ygdata.Node):
        """Wrap the user provided transform method to convert from gdata to
        modeled input and back to gdata
        """
        modeled_input = stratoweave_tmf__tmf_store__tmf640__service_entry.from_gdata(i)

        return self.transform(modeled_input).to_gdata(), None

    mut def transform_xml(self, i: xml.Node, linked: ygdata.Node, memory: ?ygdata.Node, dynstate: ?ygdata.Node) -> (ygdata.Node, ?ygdata.Node):
        """Wrap the user provided transform method to convert from XML to
        modeled input and return gdata
        """
        gdata_input = yxml.from_xml(SRC_DNODE, i, loose=False, root_path=['stratoweave-tmf:tmf-store', 'tmf640', 'service'])
        modeled_input = stratoweave_tmf__tmf_store__tmf640__service_entry.from_gdata(gdata_input)

        return self.transform(modeled_input).to_gdata(), None

class L3VpnVpnService(ttt.TransformFunction):
    transform: mut(ietf_l3vpn_svc__l3vpn_svc__vpn_services__vpn_service_entry) -> yadata.MNode

    @staticmethod
    def input_type():
        """Return the modeled input type for this transform"""
        return ietf_l3vpn_svc__l3vpn_svc__vpn_services__vpn_service_entry

    mut def transform_wrapper(self, i: ygdata.Node, linked: ygdata.Node, memory: ?ygdata.Node, dynstate: ?ygdata.Node) -> (ygdata.Node, ?ygdata.Node):
        """Wrap the user provided transform method to convert from gdata to
        modeled input and back to gdata
        """
        modeled_input = ietf_l3vpn_svc__l3vpn_svc__vpn_services__vpn_service_entry.from_gdata(i)

        return self.transform(modeled_input).to_gdata(), None

    mut def transform_xml(self, i: xml.Node, linked: ygdata.Node, memory: ?ygdata.Node, dynstate: ?ygdata.Node) -> (ygdata.Node, ?ygdata.Node):
        """Wrap the user provided transform method to convert from XML to
        modeled input and return gdata
        """
        gdata_input = yxml.from_xml(SRC_DNODE, i, loose=False, root_path=['ietf-l3vpn-svc:l3vpn-svc', 'vpn-services', 'vpn-service'])
        modeled_input = ietf_l3vpn_svc__l3vpn_svc__vpn_services__vpn_service_entry.from_gdata(gdata_input)

        return self.transform(modeled_input).to_gdata(), None

class L3VpnSite(ttt.TransformFunction):
    transform: mut(ietf_l3vpn_svc__l3vpn_svc__sites__site_entry) -> yadata.MNode

    @staticmethod
    def input_type():
        """Return the modeled input type for this transform"""
        return ietf_l3vpn_svc__l3vpn_svc__sites__site_entry

    @staticmethod
    def state_type():
        """Return the modeled operational state type for this transform"""
        return ietf_l3vpn_svc__l3vpn_svc__sites__site_entry__oper

    @staticmethod
    def new_state(path: ttt.Path):
        """Create modeled operational state for this transform path"""
        return ietf_l3vpn_svc__l3vpn_svc__sites__site_entry__oper.from_gdata(ttt.path_key_data(path))

    mut def transform_wrapper(self, i: ygdata.Node, linked: ygdata.Node, memory: ?ygdata.Node, dynstate: ?ygdata.Node) -> (ygdata.Node, ?ygdata.Node):
        """Wrap the user provided transform method to convert from gdata to
        modeled input and back to gdata
        """
        modeled_input = ietf_l3vpn_svc__l3vpn_svc__sites__site_entry.from_gdata(i)

        return self.transform(modeled_input).to_gdata(), None

    mut def transform_xml(self, i: xml.Node, linked: ygdata.Node, memory: ?ygdata.Node, dynstate: ?ygdata.Node) -> (ygdata.Node, ?ygdata.Node):
        """Wrap the user provided transform method to convert from XML to
        modeled input and return gdata
        """
        gdata_input = yxml.from_xml(SRC_DNODE, i, loose=False, root_path=['ietf-l3vpn-svc:l3vpn-svc', 'sites', 'site'])
        modeled_input = ietf_l3vpn_svc__l3vpn_svc__sites__site_entry.from_gdata(gdata_input)

        return self.transform(modeled_input).to_gdata(), None

class Router(ttt.TransformFunction):
    transform: mut(netinfra__netinfra__router_entry, linked: ygdata.Node) -> yadata.MNode

    @staticmethod
    def input_type():
        """Return the modeled input type for this transform"""
        return netinfra__netinfra__router_entry

    mut def transform_wrapper(self, i: ygdata.Node, linked: ygdata.Node, memory: ?ygdata.Node, dynstate: ?ygdata.Node) -> (ygdata.Node, ?ygdata.Node):
        """Wrap the user provided transform method to convert from gdata to
        modeled input and back to gdata
        """
        modeled_input = netinfra__netinfra__router_entry.from_gdata(i)

        return self.transform(modeled_input, linked).to_gdata(), None

    mut def transform_xml(self, i: xml.Node, linked: ygdata.Node, memory: ?ygdata.Node, dynstate: ?ygdata.Node) -> (ygdata.Node, ?ygdata.Node):
        """Wrap the user provided transform method to convert from XML to
        modeled input and return gdata
        """
        gdata_input = yxml.from_xml(SRC_DNODE, i, loose=False, root_path=['netinfra:netinfra', 'router'])
        modeled_input = netinfra__netinfra__router_entry.from_gdata(gdata_input)

        return self.transform(modeled_input, linked).to_gdata(), None

class ROADM(ttt.TransformFunction):
    transform: mut(netinfra__netinfra__roadm_entry) -> yadata.MNode

    @staticmethod
    def input_type():
        """Return the modeled input type for this transform"""
        return netinfra__netinfra__roadm_entry

    mut def transform_wrapper(self, i: ygdata.Node, linked: ygdata.Node, memory: ?ygdata.Node, dynstate: ?ygdata.Node) -> (ygdata.Node, ?ygdata.Node):
        """Wrap the user provided transform method to convert from gdata to
        modeled input and back to gdata
        """
        modeled_input = netinfra__netinfra__roadm_entry.from_gdata(i)

        return self.transform(modeled_input).to_gdata(), None

    mut def transform_xml(self, i: xml.Node, linked: ygdata.Node, memory: ?ygdata.Node, dynstate: ?ygdata.Node) -> (ygdata.Node, ?ygdata.Node):
        """Wrap the user provided transform method to convert from XML to
        modeled input and return gdata
        """
        gdata_input = yxml.from_xml(SRC_DNODE, i, loose=False, root_path=['netinfra:netinfra', 'roadm'])
        modeled_input = netinfra__netinfra__roadm_entry.from_gdata(gdata_input)

        return self.transform(modeled_input).to_gdata(), None

class BBLink(ttt.TransformFunction):
    transform: mut(netinfra__netinfra__backbone_link_entry) -> yadata.MNode

    @staticmethod
    def input_type():
        """Return the modeled input type for this transform"""
        return netinfra__netinfra__backbone_link_entry

    @staticmethod
    def state_type():
        """Return the modeled operational state type for this transform"""
        return netinfra__netinfra__backbone_link_entry__oper

    @staticmethod
    def new_state(path: ttt.Path):
        """Create modeled operational state for this transform path"""
        return netinfra__netinfra__backbone_link_entry__oper.from_gdata(ttt.path_key_data(path))

    mut def transform_wrapper(self, i: ygdata.Node, linked: ygdata.Node, memory: ?ygdata.Node, dynstate: ?ygdata.Node) -> (ygdata.Node, ?ygdata.Node):
        """Wrap the user provided transform method to convert from gdata to
        modeled input and back to gdata
        """
        modeled_input = netinfra__netinfra__backbone_link_entry.from_gdata(i)

        return self.transform(modeled_input).to_gdata(), None

    mut def transform_xml(self, i: xml.Node, linked: ygdata.Node, memory: ?ygdata.Node, dynstate: ?ygdata.Node) -> (ygdata.Node, ?ygdata.Node):
        """Wrap the user provided transform method to convert from XML to
        modeled input and return gdata
        """
        gdata_input = yxml.from_xml(SRC_DNODE, i, loose=False, root_path=['netinfra:netinfra', 'backbone-link'])
        modeled_input = netinfra__netinfra__backbone_link_entry.from_gdata(gdata_input)

        return self.transform(modeled_input).to_gdata(), None

class OpticalLink(ttt.TransformFunction):
    transform: mut(netinfra__netinfra__optical_link_entry) -> yadata.MNode

    @staticmethod
    def input_type():
        """Return the modeled input type for this transform"""
        return netinfra__netinfra__optical_link_entry

    mut def transform_wrapper(self, i: ygdata.Node, linked: ygdata.Node, memory: ?ygdata.Node, dynstate: ?ygdata.Node) -> (ygdata.Node, ?ygdata.Node):
        """Wrap the user provided transform method to convert from gdata to
        modeled input and back to gdata
        """
        modeled_input = netinfra__netinfra__optical_link_entry.from_gdata(i)

        return self.transform(modeled_input).to_gdata(), None

    mut def transform_xml(self, i: xml.Node, linked: ygdata.Node, memory: ?ygdata.Node, dynstate: ?ygdata.Node) -> (ygdata.Node, ?ygdata.Node):
        """Wrap the user provided transform method to convert from XML to
        modeled input and return gdata
        """
        gdata_input = yxml.from_xml(SRC_DNODE, i, loose=False, root_path=['netinfra:netinfra', 'optical-link'])
        modeled_input = netinfra__netinfra__optical_link_entry.from_gdata(gdata_input)

        return self.transform(modeled_input).to_gdata(), None

class GlobalSettings(ttt.TransformFunction):
    transform: mut(netinfra__netinfra__global_settings) -> yadata.MNode

    @staticmethod
    def input_type():
        """Return the modeled input type for this transform"""
        return netinfra__netinfra__global_settings

    mut def transform_wrapper(self, i: ygdata.Node, linked: ygdata.Node, memory: ?ygdata.Node, dynstate: ?ygdata.Node) -> (ygdata.Node, ?ygdata.Node):
        """Wrap the user provided transform method to convert from gdata to
        modeled input and back to gdata
        """
        modeled_input = netinfra__netinfra__global_settings.from_gdata(i)

        return self.transform(modeled_input).to_gdata(), None

    mut def transform_xml(self, i: xml.Node, linked: ygdata.Node, memory: ?ygdata.Node, dynstate: ?ygdata.Node) -> (ygdata.Node, ?ygdata.Node):
        """Wrap the user provided transform method to convert from XML to
        modeled input and return gdata
        """
        gdata_input = yxml.from_xml(SRC_DNODE, i, loose=False, root_path=['netinfra:netinfra', 'global-settings'])
        modeled_input = netinfra__netinfra__global_settings.from_gdata(gdata_input)

        return self.transform(modeled_input).to_gdata(), None
