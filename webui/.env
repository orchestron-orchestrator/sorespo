# Default values for $env/static/public imports. PUBLIC_DEMO=1 switches the
# build to the standalone demo (static adapter, hash router, mock data layer);
# use `bun run build:demo` rather than setting it here.
PUBLIC_DEMO=0
