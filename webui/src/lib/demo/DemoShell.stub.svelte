<!-- Substituted for $lib/demo/DemoShell.svelte in non-demo builds (see
vite.config.ts). Never mounted: the PUBLIC_DEMO guard in +layout.svelte is
statically false, but the dynamic import() target must still resolve so this
keeps the emitted chunk empty instead of dragging in the demo fixtures. -->
